model:
  default: poolside/laguna-s-2.1:free
  provider: nous
skills:
  external_dirs:
    - ~/.hermes/skills
plugins:
  enabled: []
_config_version: 39
agent: {}
onboarding:
  seen:
    tool_progress_prompt: true

fallback_providers:
  - provider: opencode-zen
    model: opencode/free
  - provider: kilo
    model: kilo/free
